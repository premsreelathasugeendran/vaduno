node_modules/
dist/
*.log
.env
.env.*
demo-ledger.jsonl

# Next.js dashboard
.next/
next-env.d.ts
apps/dashboard/data/
*.tsbuildinfo

# Internal launch notes and market research — not for publication
docs/LAUNCH.md
docs/research/

# Stray npm pack output — never commit tarballs
*.tgz
packcheck/
vad/
.claude/worktrees/

# Live-hook session artifacts (examples/claude-code-hook): captured payloads,
# decision logs and the file-backed cap state. Evidence from one session, not
# fixtures — regenerate by running the hook, never commit.
examples/claude-code-hook/.state/
examples/claude-code-hook/observed-payloads.jsonl
examples/claude-code-hook/guard-hook.log
