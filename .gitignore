node_modules/
dist/
*.log
.env
.env.*
demo-ledger.jsonl

# Next.js dashboard
.next/
next-env.d.ts
apps/dashboard/data/
*.tsbuildinfo

# Internal launch notes and market research — not for publication
docs/LAUNCH.md
docs/research/

# Stray npm pack output — never commit tarballs
*.tgz
packcheck/
vad/
# Local harness/editor config: machine-specific paths, not project source.
.claude/

# Live-hook session artifacts (examples/cli-agent-hook): captured payloads,
# decision logs and the file-backed cap state. Evidence from one session, not
# fixtures — regenerate by running the hook, never commit.
examples/cli-agent-hook/.state/
examples/cli-agent-hook/observed-payloads.jsonl
examples/cli-agent-hook/guard-hook.log

# Throwaway testnet wallet for examples/x402-live. Worthless faucet tokens on
# one testnet, but a private key is a private key — never commit it.
examples/x402-live/.wallet
examples/x402-live/ledger.jsonl
examples/x402-live/last-settlement.json

# Same, for examples/guarded-signer — these are per-run artifacts (the hash
# chain and the last settlement receipt), not source. It reuses the x402-live
# wallet, so there is no key of its own to ignore here.
examples/guarded-signer/ledger.jsonl
examples/guarded-signer/ledger-deny.jsonl
examples/guarded-signer/last-settlement.json
