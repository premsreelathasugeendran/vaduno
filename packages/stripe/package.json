{
  "name": "@vaduno/stripe",
  "version": "0.7.0",
  "description": "Stripe Issuing rail adapter for Vaduno. Makes the guard the real-time authorization brain for card charges — approve/decline each issuing_authorization.request by policy, within Stripe's 2s window, hash-chained. Never holds keys to funds or card PANs.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/premsreelathasugeendran/vaduno.git",
    "directory": "packages/stripe"
  },
  "license": "MIT",
  "keywords": [
    "stripe",
    "stripe-issuing",
    "ai-agents",
    "payments",
    "spend-control",
    "real-time-authorization"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "prepublishOnly": "npm run build && npm run test"
  },
  "dependencies": {
    "@vaduno/guard": "^0.7.0"
  },
  "peerDependencies": {
    "stripe": ">=17"
  },
  "peerDependenciesMeta": {
    "stripe": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.5.4",
    "vitest": "^3.2.6"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
